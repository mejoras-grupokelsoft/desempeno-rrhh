      - name: Ping Supabase REST API
        run: |
          echo "DEBUG: Secrets exist"
          echo "URL length: ${#SUPABASE_URL}"
          echo "KEY length: ${#ANON_KEY}"
          
          SUPABASE_URL="${{ secrets.VITE_SUPABASE_URL }}"
          ANON_KEY="${{ secrets.VITE_SUPABASE_ANON_KEY }}"
          
          echo "After assignment - URL length: ${#SUPABASE_URL}"
          echo "After assignment - KEY length: ${#ANON_KEY}"
          
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "$SUPABASE_URL/rest/v1/users?select=id&limit=1" \
            -H "apikey: $ANON_KEY")
          
          echo "Supabase ping status: $STATUS"
          if [ "$STATUS" = "200" ] || [ "$STATUS" = "206" ]; then
            echo "✓ Supabase is alive"
            exit 0
          else
            echo "✗ Supabase returned status $STATUS"
            exit 1
          fi
